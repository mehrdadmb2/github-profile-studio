# Setup

Upload `index.html` to a GitHub Pages repository.

Suggested repository name:

`github-profile-studio`

Suggested description:

`Premium GitHub profile & README builder with live public profile data, themes, visual presets and Markdown export.`

For a profile README, create a public repository whose name exactly matches the GitHub username and paste the exported README.md into its root.
